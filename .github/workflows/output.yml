name: Output geosite.dat

on:
  workflow_dispatch:
  schedule:
    - cron: "00 18 * * *"
  push:
    branches:
      - master
    paths-ignore: 
      - "**/README.md" 

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
  
    steps:
      - name: Setup Go 1.x.y
        uses: actions/setup-go@v2
        with:
          go-version: ^1.16
          
      - name: Set variables
        run: |
          echo "RELEASE_NAME=$(date +%Y%m%d%H%M)" >> $GITHUB_ENV
        shell: bash
          
      - name: Checkout codebase
        uses: actions/checkout@v2.3.4

      - name: Build geosite.dat file
        run: |
          curl -L https://raw.githubusercontent.com/CalmLong/custom-block/main/block.txt -o block.txt
          
          mkdir -p ./publish/v2ray/default
          go run geosite -D=true
          mv geosite.dat ./publish/v2ray/default
          
          mkdir -p ./publish/v2ray/full
          go run geosite -D=true -f=true
          mv geosite.dat ./publish/v2ray/full
          
          mkdir -p ./publish/v2ray/domain
          go run geosite -D=true -d=true
          mv geosite.dat ./publish/v2ray/domain
          
          cp block.txt ./publish/v2ray/

      - name: Build clash.yaml file
        run: |
          mkdir -p ./publish/clash/
          go run geosite -D=true -F=clash
          mv *.yaml ./publish/clash/

          cp block.txt ./publish/clash/

      - name: Clear
        run: |
          rm block.txt

      - name: Git push assets to "release" branch
        run: |
          cd publish || exit 1
          git init
          git config --local user.name "github-actions[bot]"
          git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout -b release
          git add --all
          git commit -m "${{ env.RELEASE_NAME }}"
          git remote add geosite "https://${{ github.actor }}:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}"
          git push -f geosite release
          
      - name: Delete publish directory
        run: |
          rm -rf publish
